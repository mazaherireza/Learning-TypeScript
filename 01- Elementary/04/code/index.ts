const person: {
  firstName: string;
  lastName: string;
  age: number;
  married: boolean;
} = {
  firstName: "Reza",
  lastName: "Mazaheri",
  age: 35,
  married: false,
};

console.log(`${person.firstName} ${person.lastName}`);
